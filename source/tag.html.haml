%ul.collection.with-header
  %li.collection-header
    %h4 Articles tagged #{tagname}
  - page_articles.each_with_index do |article, i|
    %li.collection-item
      = link_to article.title, article
      %span= article.date.strftime('%b %e')
